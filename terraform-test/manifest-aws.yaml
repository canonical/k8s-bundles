k8s:
  units: 3
  base: ubuntu@24.04
  constraints: arch=amd64 cores=2 mem=8G root-disk=16G
  channel: latest/edge
k8s-worker:
  units: 3
  base: ubuntu@24.04
  constraints: arch=amd64 cores=2 mem=8G root-disk=16G
  channel: latest/edge
aws-integrator:
  channel: 1.31/stable
  base: ubuntu@22.04
aws-k8s-storage: {}
aws-cloud-provider: {}
