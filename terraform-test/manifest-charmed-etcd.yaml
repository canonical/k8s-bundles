k8s:
  units: 1
  base: ubuntu@24.04
  constraints: arch=amd64 cores=2 mem=8192M root-disk=16384M
  channel: latest/edge/charmed-etcd
  config:
    bootstrap-datastore: etcd
k8s-worker:
  units: 1
  base: ubuntu@24.04
  constraints: arch=amd64 cores=2 mem=8192M root-disk=16384M
  channel: latest/edge/charmed-etcd
charmed-etcd:
  units: 1
  channel: 3.6/edge
  base: ubuntu@24.04
